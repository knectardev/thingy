Context: The backend routing, database ingestion, and logging for the "Thingy" capture system are fully implemented and verified via tests.

Task: Build the frontend UI in @app/page.tsx to serve as the "remote control" for this system.

UI Requirements:

Capture Interface:

A clean, mobile-first textarea with a placeholder (e.g., "Capture a thought...").

A primary "Submit" button.

On submit:

Disable the button to prevent double-clicks.

POST the text content to /api/ingest.

Clear the textarea upon success and briefly show a success state.

Activity Feed:

Beneath the input, create a real-time list of recent activity.

Use useEffect and setInterval (or a SWR/React Query pattern) to poll GET /api/logs every 5 seconds.

Display the log entries in a clean list, showing the action and status for the last 50 captures.

Technical Constraints (Strictly Follow .cursorrules):

Styling: Use Tailwind CSS for a minimalist, accessible, mobile-first design.

Reliability: Wrap API calls in try/catch blocks.

Types: Use a Log interface for the activity feed data.

Structure: Keep components modular if you decide to separate the Feed and the Input into distinct sub-components.

Start by analyzing the current state of @app/page.tsx and replacing the default template with this new interface.

Additional details: 

The "Double-Click" Defense: Since you built idempotency into your backend, remind the AI to include a client-side UUID in the POST request body so the UI perfectly matches the backend safety logic you already implemented.

Empty State: Please add an "Empty State" message for the Activity Feed so the UI looks clean before you've made your first capture.