Context: I have a Node.js script that previously imported complex, semi-structured backlog data into GitHub issues. I want to modernize this into a simple, reusable utility for the "Thingy" app.

Task: Create a new file lib/scripts/import-lendl.ts that accomplishes the following:

Trigger: It should accept a raw text input (from a file or stdin). The script should parse this text to find items tagged with #lendl task or #lendle task.

Handler Integration: Instead of complex GitHub CLI orchestration, the script should iterate over the extracted items and call my existing lib/router.ts route() function for each item.

This way, if a user tags a block of text with #lendl task, the router will process each task according to my existing "Thingy" handlers.

Idempotency: Since I am using an idempotency key (UUID) in my system, please ensure this script generates a unique UUID for each imported task so that the /api/ingest logic (or the handler logic) can prevent duplicates if run multiple times.

Logging: Use the existing lib/logger.ts or console.log for status updates (e.g., "Imported X tasks").

Simplicity: Remove all the GitHub-specific dependencies (execSync, yaml, gh-cli). Keep it as a pure TypeScript utility script that runs in the project environment.

Constraint: Please adhere to the architectural rules in .cursorrules—specifically, keep the code modular, use typed interfaces, and wrap operations in try/catch.