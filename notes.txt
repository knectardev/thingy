Context: My "Thingy" app is fully functional with modular handlers for GitHub and Google Sheets. I now need to add a new destination: Gmail.

Task: Implement lib/handlers/gmail.ts and integrate it into the capture pipeline.

Requirements:

Trigger: The parser must support a new #email token (e.g., "Draft a response to John #email"). Update lib/parser.ts to recognize this new token.

Handler Implementation:

Create lib/handlers/gmail.ts using the nodemailer library (or the Gmail API via googleapis).

The handler should send an email to a configurable address (e.g., process.env.GMAIL_TARGET_ADDRESS or my own address) with the capture content as the email body.

Use a persistent Service Account or OAuth2 flow for authentication (add placeholders for GMAIL_CLIENT_ID, GMAIL_CLIENT_SECRET, and GMAIL_REFRESH_TOKEN in .env.local).

Router & UI:

Add the email token to lib/router.ts.

Update the ActivityFeed UI in app/components/ActivityFeed.tsx to include email: "Gmail" in TOKEN_DESTINATIONS.

Logging: Adhere to .cursorrules by adding logger.log() at the start and end of the handler.

Reliability: Wrap the email sending logic in a try/catch block to ensure that a failure to send an email does not halt the ingestion process.

Constraint: Please analyze the existing handler implementations (like @lib/handlers/github.ts) and ensure this new Gmail handler matches the same asynchronous, logged, and typed interface.

Important Considerations:
Authentication: Unlike the Google Sheets service account (which uses a JSON key), Gmail integration typically requires an OAuth2 flow to get a refresh_token if you are sending as yourself. If you are using a workspace account, a service account with "Domain-Wide Delegation" is an alternative.

Testing: Just like your other handlers, remember to add a mock test in tests/handlers.test.ts to ensure the router correctly dispatches to the handleGmail function.

Environment: Make sure to document these new Gmail credentials in your .env.local just like you did for your Sheets integration.